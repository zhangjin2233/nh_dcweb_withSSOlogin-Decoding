const options = {
  codeMode: 'clike/x-java', //可选项：'javascript' | 'vue' | 'html'，clike语言(如'clike/x-java') | sql语言(如'sql/x-sql')
  codeOptions: {
    indentUnit: 2, //缩进单位
    smartIndent: true, //智能缩进
    tabSize: 2, //tab缩进
    readOnly: false,
    showCursorWhenSelecting: true,
    lineNumbers: true, //显示行号
  }
}

export default options